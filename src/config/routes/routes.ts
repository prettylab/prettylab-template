import { Route } from "@prettylab/core/types/Route";

export const routes: Record<string, Route> = {
  index: {
    href: "/",
    label: "Main page",
  },
  login: {
    href: "/login",
    label: "Login",
  },
};
